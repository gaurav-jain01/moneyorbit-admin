import React, {useState, useEffect} from "react";
import { useParams } from "react-router-dom"; 

import {
  Box,
  Typography,
  Card,
  CardContent,
  Chip,
  Divider,
  CircularProgress,
} from "@mui/material";

export default function ipoDetail({id}) {
    return
}